<resources>
    <style name="Theme.PasswordManager" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <!-- Primary brand color -->
        <item name="colorPrimary">@color/colorPrimaryStart</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryEnd</item>
        <item name="colorOnPrimary">@color/colorWhite</item>

        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorSecondaryVariant">@color/colorHighlight</item>
        <item name="colorOnSecondary">@color/colorBlack</item>

        <!-- Background color -->
        <item name="android:windowBackground">@color/colorBackground</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/colorPrimaryEnd</item>
        <item name="android:navigationBarColor">@color/colorBackground</item>

        <!-- Fix for TextInputLayout hints & password toggle -->
        <item name="colorControlNormal">@color/colorDarkBlue</item>  <!-- eye icon color -->
        <item name="colorOnSurface">@color/colorBlack</item>         <!-- hint/text color -->
    </style>
</resources>